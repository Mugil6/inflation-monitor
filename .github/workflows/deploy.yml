name: Deploy to Docker Hub

on:
  push:
    branches:
      - main
    # EXCLUDE these files from triggering a Docker build
    paths-ignore:
      - 'README.md'
      - 'app/**'        # Ignore the Streamlit folder
      - '.gitignore'
      - 'LICENSE'
  
  # Keep this so you can still trigger it manually
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: actions/checkout@v4
        # (Your existing Buildx setup steps here...)

      - name: Log in to Docker Hub
        # (Your existing Docker Hub login using secrets here...)

      - name: Build and Push
        # (Your existing build and push command here...)